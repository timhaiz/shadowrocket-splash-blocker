{
  "name": "shadowrocket-splash-blocker",
  "version": "1.0.0",
  "private": true,
  "description": "Conservative Shadowrocket splash ad blocker for selected iOS apps",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
